defmodule FlappyBirdWeb.LayoutViewTest do
  use FlappyBirdWeb.ConnCase, async: true

  # Lors du test des assistants, vous souhaiterez peut-être importer Phoenix.HTML et
  # utilise des fonctions telles que safe_to_string() pour convertir l'assistant
  # résultat dans une chaîne HTML.
  # importer Phénix.HTML
end
